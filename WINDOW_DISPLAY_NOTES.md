# HOOP HAVOC — 窗口缩放与全屏修复说明

分支：`fix-fullscreen`
目标：修掉「拖大窗口画面挤在左上角」，以及全屏渲染器候选里的 macOS 专有项
测试环境：Windows 11 Pro (22631) / Python 3.13.7 / pygame 2.6.1 (SDL 2.28.4)
显示器：双 1920×1080，均为 100% 缩放

改 2 个源文件、新增 1 个文件。`ReadMe.MD` 未动。

---

## 一、结论先讲

原方案（给窗口模式加 `pygame.SCALED`）方向是对的，但**直接加上去会坏三个地方**，
三个坑都不是看代码能看出来的，是实测撞出来的：

1. **pygame 的 `SCALED` 默认开整数缩放**。加了 `SCALED` 之后，把窗口拖到
   1440×810，画面**不会**放大填满，而是原样 1:1 摆在正中间、四周一圈厚黑边。
   比「挤在左上角」好，但离「等比填满」还差得远。
2. **加了 `SCALED` 之后，原来的全屏切换代码会直接抛异常**。窗口和全屏都带
   `SCALED` 时，再调 `set_mode` 切换会报 `failed to create renderer`，而且失败后
   display 处于半坏状态。原代码正是用 `set_mode` 切的。
3. **pygame 会把新建的 `SCALED` 窗口自动撑大**到桌面能容纳的最大整数倍。
   1080p 屏上启动就是 1920×1080，比工作区还高，标题栏顶到屏幕外。

三个都修掉了，7 条验收标准全部实测通过。

---

## 二、改了什么

### 1. `display.py`（新增）— 显示层独立成模块

原来 `main.py` 里那个 72 行的 `create_screen()` 搬进了新模块并重写。
`main.py` 从 180 行缩到 108 行，只剩游戏流程。

模块干四件事：

| 函数 | 作用 |
|---|---|
| `prepare_display()` | 在 `pygame.init()` **之前**设 SDL hint |
| `create_screen(fullscreen)` | 建 display，带多层回退 |
| `set_fullscreen(on, allow_rebuild)` | 运行期切换全屏 |
| `renderer_candidates()` | 分平台的渲染器候选表 |

### 2. 关掉整数缩放 —— 这是「填满窗口」的关键

```python
os.environ.setdefault("SDL_HINT_RENDER_SCALE_QUALITY", "linear")
```

pygame 的判断逻辑是**这个 hint 有没有被设过**，而不是设成了什么值：
没设 → 整数缩放；设成 `nearest` / `linear` / `best` 任意一个 → 按任意比例等比缩放。

实测对比（同一台机器，真实截屏量出来的渲染区）：

| 窗口 | 不设 hint | 设了 hint |
|---|---|---|
| 1440×810 | `(240,135,960,540)` 1.0× 居中，四周黑边 | `(0,0,1440,810)` **1.50× 铺满** |
| 1600×600 | `(320,30,960,540)` 1.0× 居中 | `(267,0,1066,600)` **1.11× 左右黑边** |
| 1200×900 | `(120,180,960,540)` 1.0× 居中 | `(0,112,1200,675)` **1.25× 上下黑边** |

选 `linear` 不选 `nearest`：这游戏是手绘风格美术，不是严格像素画，非整数倍下
`nearest` 会出现像素大小不均的锯齿。**两者帧率实测完全一样**（见验收 7），
所以这纯是观感选择。玩家想要锐利边缘可以自己设环境变量覆盖成 `nearest`。

### 3. 全屏切换改用 `toggle_fullscreen()`

原代码用 `set_mode` 重建 display 来切全屏。窗口模式加上 `SCALED` 之后这条路走不通了：

```
round 1 fullscreen: OK
round 1 windowed  : FAILED: failed to create renderer     ← 第一次就炸
```

换成 `pygame.display.toggle_fullscreen()`，6 轮往返全过。
`set_mode` 重建保留为回退路径（`toggle_fullscreen` 抛异常时才走）。

`toggle_fullscreen` 还有个关键好处：**它保持同一个 surface 对象**
（实测 6 轮往返 `id()` 不变，且用旧引用继续绘制正常）。这正是「设置页里切全屏
能立刻生效」的前提——见下一条。

### 4. 设置页切全屏立即生效（`ui.py`，2 处改动）

原来的行为是：设置页里点 Fullscreen 只翻转一个布尔值，要等**退出设置页**回到
`main.py` 才真正切换。玩家点了没反应，像坏了。

```python
elif item == "fullscreen":
    settings.fullscreen = not settings.fullscreen
    set_fullscreen(settings.fullscreen, allow_rebuild=False)   # 新增
```

`allow_rebuild=False` 是有意的：设置页的循环手里攥着 `screen` 引用还在画，
如果这次切换需要重建 display，旧 surface 会失效、当场崩。所以只做「就地切换」，
真需要重建的情况留给 `main.py` 在设置页返回后收拾（那段逻辑原样保留，且幂等）。

### 5. 分平台渲染器候选

```python
_RENDERER_CANDIDATES = {
    "darwin": ("metal", "opengl", "software"),                    # 一字未动
    "win32":  ("direct3d11", "direct3d", "opengl", "software"),
}
```

实测这台机器 SDL 报告的可用后端是
`direct3d / direct3d11 / direct3d12 / opengl / opengles2 / software`，
确实没有 `metal`。跳过 `direct3d12`，SDL2 的 D3D12 后端偏新、不如 D3D11 稳。

首个成功的后端会记下来，后续重建 display 不用再探一遍。

### 6. 顺手带上 DPI 感知（`version-windows` 分支转过来的）

```python
if sys.platform == "win32":
    os.environ.setdefault("SDL_WINDOWS_DPI_AWARENESS", "permonitorv2")
```

SDL 2.28 默认让进程保持 DPI unaware，125%/150% 缩放的笔记本上 Windows 会把整个
窗口位图拉伸，美术会糊。实测这行能把进程翻成 `PER_MONITOR_AWARE`。

DPI aware 之后 SDL 按物理像素算窗口，所以初始窗口尺寸也按显示器 DPI 等比放大
（`_default_window_size()`），否则 150% 屏上「960×540 窗口」看起来只有三分之二大。

**本机是 100% 缩放，这条只能验证「没有引入回归」，验证不了「高 DPI 屏上更清晰」。**
需要一台 125%/150% 的机器复核。

---

## 三、一个花了很久的坑：SDL 会在几秒后突然崩掉

加完上面这些，游戏能跑、能缩放、能切全屏，但**关闭时必定 `0xC0000005`（访问违规）**，
而且崩溃点飘忽（有时在 `ui.py` 的 `_draw_panel`，有时在 `event.get()`），
基线版本则干干净净。

排查过程走了几个弯路，值得记下来免得以后再踩：

- 一度以为是 DPI awareness 引起的。**是错的**——那组对照每组只跑了 1 次，
  而这个崩溃是概率性的，样本量不够。后来每组跑 5 次才看清。
- 一度以为是「在事件循环中途改窗口尺寸」。也不对。
- 中途 `.venv` 被删掉过一次，导致一次假崩溃（DLL running 时被删），
  白查了一轮。

真正的原因是：

> **`pygame._sdl2.video.Window.size = ...` 在 `set_mode` 之后早期调用会破坏 SDL
> 的窗口状态**，进程会在几秒后的某个无关操作上崩掉。

而这个调用是必须的——就是用来把 pygame 自动撑大的窗口收回 960×540 的（坑 3）。

最终对照数据（每组 5 次，其它条件全部相同）：

| 初始收缩窗口的方式 | 崩溃 |
|---|---|
| `_sdl2 Window.size`，`set_mode` 后立刻 | **5 / 5** |
| `_sdl2 Window.size`，推迟到主循环第一帧之后 | 0 / 5 |
| 完全不收缩 | 0 / 5 |
| **改用 Win32 `SetWindowPos` 驱动 HWND** | **0 / 5** |
| 基线仓库（未改动） | 0 / 5 |

推迟到第一帧之后虽然不崩，但资源加载要 3.5 秒，玩家会看着一个铺满屏幕的窗口
杵在那儿 3.5 秒再突然缩小——实测过，很难看。

所以 Windows 上改走 Win32：

```python
def _resize_window(size):
    if sys.platform == "win32" and _resize_window_win32(size):
        return True
    ...  # 其它平台回退到 pygame._sdl2
```

换完之后：启动瞬间（t+0.00s）窗口就是 960×540，无跳变，0/5 崩溃。
DPI awareness 也一并复测过，`permonitorv2` + Win32 收缩 = 0/5 崩溃。

---

## 四、验收标准逐条实测

测试方式说明：**全部是驱动真实游戏进程**跑出来的，不是单元测试。
用 Win32 API 从外部枚举游戏窗口、注入真实鼠标手势（拖标题栏、拖右下角改变大小、
双击标题栏最大化、点击菜单项）、用 GDI 抓真实屏幕像素，再从截屏里量出画面
实际占据的矩形，和「等比缩放应有的矩形」逐项比对。

有一点值得提醒：**从外部进程调 `SetWindowPos` 改游戏窗口几何会把游戏打死**
（基线版本也一样），所以测试全部改走模拟真实鼠标拖拽的路径。这是测试方法的
限制，不是游戏的缺陷——真人拖窗口不会触发。

### 1. 启动默认 960×540 窗口 ✅

```
t+0.00s  client=(960, 540)      ← 启动瞬间即为逻辑尺寸，无跳变
```

专门写了个脚本每 0.25 秒采一次窗口尺寸，从进程启动一直采到稳定。

### 2. 任意拖拽窗口，等比缩放填满、保持宽高比、多余处黑边 ✅

真实拖右下角调整大小，每个尺寸截屏并量出渲染区：

| 拖到 | 实际客户区 | 量出的渲染区 | 缩放 | 宽高比 | 判定 |
|---|---|---|---|---|---|
| 1440×810 | 1440×810 | `(0,0,1440,810)` | 1.500 | 1.778 | 铺满 ✅ |
| 1600×620 | 1600×620 | `(249,0,1102,620)` | 1.148 | 1.777 | 左右黑边各 249 ✅ |
| 1180×900 | 1180×900 | `(0,118,1180,663)` | 1.229 | 1.780 | 上下黑边各 118 ✅ |
| 1024×700 | 1024×700 | `(0,62,1024,576)` | 1.067 | 1.778 | 上下黑边各 62 ✅ |

宽高比全部是 1.778（16:9），**没有出现左上角一小块的情况**。截图存档已肉眼逐张确认。

### 3. 最大化按钮 ✅

双击标题栏最大化：

```
maximized   client=1920×1057   渲染区 (20,0,1879,1057)   scale=1.957   左右黑边各 20
restored    client=1024×700    渲染区 (0,62,1024,576)    正确还原
```

**附带发现**：`SCALED` 下窗口**无法**被缩到 960×540 以下（SDL 设了最小尺寸）。
实测拖向 480×270 会被卡在 960×540。我认为这个行为可以接受——960×540 在任何
能跑这游戏的屏幕上都放得下——但这是新增的限制，写在这里备案。

### 4. 缩放后菜单点击热区与悬停高亮 ✅

**这是整个方案的承重假设，做了两层验证。**

第一层，纯坐标层：真实移动系统光标到窗口内已知物理点，读游戏拿到的逻辑坐标，
和「按当前 viewport 应该换算出的值」比对。覆盖 1.000 / 1.111 / 1.250 / 1.500 /
1.957 五种缩放比，每种 4 个点：

```
最大误差 = 1.0 逻辑像素（纯取整误差）
```

`pygame.mouse.get_pos()` 和 `MOUSEBUTTONDOWN.pos` 都验证了。

顺带一个好性质：落在黑边里的点击会得到负值或超界坐标（如 `(-96,485)`、`(96,558)`），
不会被错误吸附到边缘按钮上。

第二层，真实 UI：把窗口最大化到 1920×1057（1.957 倍），依次悬停 6 个主菜单项并截屏：

- 悬停 CREDITS → **CREDITS 精确高亮**（橙框 + 箭头），零偏移
- 点击 SETTINGS → 正确进入设置页，设置页本身也正确缩放填满

### 5. 反复切换全屏/窗口 ✅

3 次独立运行 × 每次 6 轮 = **18 次切换**，全部通过：

```
toggled on the settings page: (1920,1057) -> (1920,1080)  APPLIED IMMEDIATELY
still on settings page   client=1920x1080  rendered=(0,0,1920,1080) scale=2.000  OK
toggled on the settings page: (1920,1080) -> (1920,1057)  APPLIED IMMEDIATELY
```

- 每次都**立即生效**（还在设置页上就切了，不用退出去）
- 进全屏 `scale=2.000` 完整填满、无黑边
- 退全屏精确恢复到进入前的窗口尺寸（1920×1057）
- 无黑屏、无崩溃，进程 `returncode=0` 正常退出

注：SDL 自己**不会**恢复退出全屏前的窗口尺寸（实测 4 轮，窗口一直停在
1920×1080），所以这个恢复是 `display.py` 主动做的。

### 6. Windows 上不再尝试 metal，mac 分支不变 ✅

```
darwin   -> ('metal', 'opengl', 'software')                      OK  ← 与原代码逐字一致
win32    -> ('direct3d11', 'direct3d', 'opengl', 'software')      OK
         'metal' present: False                                   OK
linux    -> ('opengl', 'software')                                OK
```

另外，实际运行时默认渲染器就能建成，根本走不到候选表循环——所以 Windows 上
不仅不试 `metal`，连一次 `display.quit()/init()` 都省了。

### 7. 帧率 ⚠️ 基本达标，但大窗口下有约 3% 开销

跑真实的 `ui.main_menu()` 循环、数真实的 `display.flip()` 次数，每档 4 秒，
resize 之后留 20 帧稳定期不计入：

| 窗口 | 缩放 | FPS（三次测量） |
|---|---|---|
| 960×540 | 1.000× | 60.3 / 60.5 / 60.7 |
| 1440×810 | 1.500× | 60.5 / 60.6 / 61.1 |
| 1920×1061 | 1.957× | 58.1 / 58.1 / 58.2 |
| 1600×620 | 1.148× | 57.4 / 57.5 / 57.9 |

`clock.tick(60)` 卡的是上限，所以 60.x 就是满帧。1920 和 1600 两档稳定在 58，
比 60 低约 3%，是 SDL 缩放呈现本身的开销。数值可复现，不是噪声。

试过把过滤方式换成 `nearest`，帧率**完全一样**（58.2 / 57.6），
所以这个开销和过滤算法无关，换不掉。

主观上 58 和 60 基本无感，而且这是主菜单（每帧要画背景、角色动画、按钮）。
如果你介意，唯一的办法是回到整数缩放——但那样就换回了「画面不填满窗口」。
我认为不值得。

---

## 五、关于 mac 版

你问能不能顺手把 mac 的全屏问题一起修了。**有几条是直接受益的，有一条需要你在
mac 上复核。**

### 直接修掉的：mac 全屏画面偏小

这条我判断就是 mac 全屏问题的主因。原代码全屏用了 `SCALED` 但没设
`SDL_HINT_RENDER_SCALE_QUALITY`，所以**整数缩放一直是开着的**。
只要显示器分辨率不是 960×540 的整数倍，画面就会缩到较小的整数倍居中显示：

| mac 常见分辨率 | 理论缩放 | 整数缩放实际取 | 后果 |
|---|---|---|---|
| 1512×982（14" MBP 默认） | 1.575 | **1×** | 960×540 小块居中，四周巨大黑边 |
| 1440×900 | 1.500 | **1×** | 同上 |
| 2560×1600 | 2.667 | **2×** | 1920×1080 居中，四周黑边 |
| 1920×1080 | 2.000 | 2× | 正好，看不出问题 |

也就是说：**在 1080p 外接屏上一切正常，一回到 MacBook 自带屏就变小**——
如果症状是这样，那就对上了。加了 hint 之后按任意比例等比填满，这条自动修好。

同理，窗口模式加 `SCALED` 之后 mac 上拖窗口也不会再错位。

### 也受益的：全屏切换更稳

原来 mac 上切全屏靠 `set_mode` 重建 display，还带一套 metal→opengl→software
的探测。现在优先走 `toggle_fullscreen()`，不重建、不销毁 renderer。
mac 的候选表一字未动，回退路径完全保留。

### ⚠️ 需要你在 mac 上复核的一条

`display.py` 收缩初始窗口时，Windows 走 Win32 `SetWindowPos`，
**其它平台回退到 `pygame._sdl2.video.Window.size`** —— 而这正是在 Windows 上
会导致延迟崩溃的那个调用（见第三节）。

我没有 mac，**验证不了 mac 上是否有同样的问题**。请在 mac 上跑一下：

```bash
python main.py          # 启动、玩一会儿、正常退出
```

正常退出即可；如果出现启动几秒后无提示地闪退、或退出时报错，那就是同一个坑。
真遇到了，两条修法任选：

1. 在 `create_screen()` 里对非 Windows 平台跳过这次收缩，
   接受 pygame 自动放大的窗口尺寸作为启动尺寸；
2. 或者把这次收缩推迟到主循环第一帧之后（在 `ui.main_menu` 的
   `pygame.display.flip()` 之后调一次），代价是启动时窗口会先大后小跳一下。

另外 DPI 那段有 `sys.platform == "win32"` 保护，mac 完全不受影响。

---

## 六、发现了但没修的问题

### 1. 从外部进程改窗口几何会打死游戏（基线也有）

外部进程对游戏窗口调 `SetWindowPos`（改尺寸**或**只是移动位置）会让游戏进程
当场 `0xC0000005`。**未改动的基线版本一样会死**，所以不是这次改动引入的。

真人拖窗口、最大化都不触发（走的是另一条消息路径，实测大量拖拽都没事）。
会踩到的场景是别的软件来摆布游戏窗口——窗口管理工具、录屏软件、
某些外挂式的窗口布局工具。概率不高，但 Steam 上用户环境五花八门，
建议知道有这么回事。

### 2. 游戏窗口初始位置不受控

实测启动时窗口位置飘忽：有时在 `(-8,-31)`（标题栏顶到屏幕外，鼠标够不到），
有时直接开在第二台显示器上。这是 SDL 默认的
`SDL_WINDOWPOS_UNDEFINED` 行为，跟这次改动无关，但对玩家不友好——
标题栏在屏幕外时窗口没法拖动。

修起来很简单（`SDL_VIDEO_CENTERED=1` 或显式指定位置），但这属于产品决策
而不是这次的 bug 修复范围，没擅自加。测试脚本里我用
`SDL_VIDEO_WINDOW_POS` 强制指定了位置才能稳定跑自动化。

### 3. 合成键盘事件送不进游戏

自动化测试时发现 `keybd_event` 注入的按键（试过 ESC）游戏收不到，鼠标注入
则完全正常。没有深究是 SDL 的输入路径还是 Windows 的注入限制——
不影响真人玩，只影响自动化测试，所以验收 5 全程改用鼠标点 BACK 完成。

### 4. `settings.json` 目前仍在 `~/.basketball_brawl/`

`version-windows` 分支把存档挪到了 `%APPDATA%\HOOP HAVOC\`，
本分支还是旧路径。两边合并后以那边为准，不冲突（我没动 `settings.py`）。

---

## 七、本次改动清单

```
 M main.py     删掉 create_screen()（72 行）改为调用 display 模块；180 → 108 行
 M ui.py       +2 处：import set_fullscreen；设置页切全屏时立即生效
?? display.py  新增：显示层（缩放、全屏、渲染器回退、DPI）
```

未改动：`ReadMe.MD`、`.gitignore`、`HOOP HAVOC.spec`、`settings.py`、
`constants.py`、`audio.py`、`localization.py`，以及全部玩法代码
（`game.py` / `player.py` / `ball.py` / `ai.py` / `arenas.py` /
`characters.py` / `feedback.py` / `animation.py`）。

`ui.py` 原本约定归 `version-windows` 分支，本轮经你同意后改了 2 处，
都集中在 `settings_menu()`，与那边的改动（`.gitignore` / spec / settings.py）
不重叠。
